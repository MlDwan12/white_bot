-- Момент гашения refresh-сессии: без него повторное обновление из второй
-- вкладки неотличимо от кражи токена, и админа выкидывало бы со всех
-- устройств за обычную перезагрузку страницы.

-- AlterTable
ALTER TABLE "AdminSession" ADD COLUMN     "revokedAt" TIMESTAMP(3);

