/**
 * Имя куки с `state` авторизации VK. Вынесено отдельным файлом: её ставит
 * `VkOAuthController` при переходе на VK, а читает панель, когда админ
 * возвращается с кодом, — и два места, знающие имя каждое по-своему,
 * разошлись бы молча.
 */
export const VK_OAUTH_STATE_COOKIE = 'vk_oauth_state';
